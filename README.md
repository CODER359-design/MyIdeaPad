<div align="center">
  <img src="./public/og-image.png" alt="CutLink preview" width="360" />
</div>

# CutLink — сервіс скорочення посилань

CutLink — це повноцінний web-сервіс типу bit.ly, який дозволяє скорочувати довгі URL, відстежувати кліки, задавати кастомні коди та контролювати термін дії посилань. Проєкт побудований на **Next.js App Router + MongoDB Atlas** і готовий до деплою на Vercel або Infinityfree (через налаштовану `BASE_URL`).

## Функціонал

- 📮 Форма для введення довгого посилання, кастомного коду та терміну дії.
- 🧠 Валідація URL + автододавання `https://`.
- ♻️ Захист від дублікатів: повертається вже існуюче коротке посилання.
- 🧾 API `POST /api/shorten`, `GET /api/stats/:code`, динамічний редирект `/go/:code`.
- 📊 Лічильник кліків інкрементується при кожному переході.
- 📋 Кнопка «Копіювати», індикатор успіху, повідомлення про помилки.
- 🔐 Підтримка обмеження терміну дії (24h, 7d, 30d, безстроково).

## Стек

- **Frontend**: Next.js 16 (App Router), React 19, Tailwind CSS 4
- **Backend**: Next.js API routes (Edge-ready), Mongoose ODM
- **DB**: MongoDB Atlas (можна використовувати будь-який MongoDB сумісний інстанс)

## Запуск локально

```bash
git clone <repo-url>
cd shortlinks
cp .env.example .env.local
npm install
npm run dev
```

Проєкт буде доступний на [http://localhost:3000](http://localhost:3000).

## Змінні середовища

| Назва         | Опис                                      |
|---------------|--------------------------------------------|
| `MONGODB_URI` | Рядок підключення до MongoDB Atlas         |
| `BASE_URL`    | Базовий URL застосунку (для коротких лінків)|

> **Порада:** для Infinityfree достатньо вказати `BASE_URL=https://ваш-домен/папка`, тоді `buildShortUrl` сформує правильну адресу незалежно від того, чи знаходиться застосунок у корені `htdocs`, чи в підпапці.

## Структура основних модулів

- `src/app/page.tsx` — SPA з формою створення коротких URL.
- `src/app/api/shorten/route.ts` — створення посилань, перевірка дублікатів, кастомні коди.
- `src/app/api/stats/[code]/route.ts` — повертає статистику (кліки, expiry, оригінальний URL).
- `src/app/go/[code]/route.ts` — редирект + оновлення лічильника кліків.
- `src/lib/db.ts` — конектор до MongoDB (кешування підключення).
- `src/lib/shortener.ts` — генерація кодів, нормалізація URL, логіка термінів дії.

## Деплой

1. **Vercel**
   - `MONGODB_URI` і `BASE_URL` вказати у Project Settings → Environment Variables.
   - Створити MongoDB Atlas cluster → передати рядок підключення.

2. **Infinityfree / shared hosting**
   - Зібрати застосунок: `npm run build` → `npm run start` (потрібен Node runtime).
   - Налаштувати reverse proxy / Node hosting. Якщо застосунок розміщується в підпапці, оновити `BASE_URL`.

## API приклади

### POST /api/shorten

```bash
curl -X POST http://localhost:3000/api/shorten \
  -H "Content-Type: application/json" \
  -d '{
        "originalUrl": "https://example.com/very/long/url?with=params",
        "customCode": "cutlink2025",
        "expiresIn": "7d"
      }'
```

### GET /api/stats/cutlink2025

```bash
curl http://localhost:3000/api/stats/cutlink2025
```

## Скриншоти

- `public/og-image.png` — головний екран (замініть своїм скриншотом для портфоліо).

## Ліцензія

MIT — використовуйте CutLink як стартову точку для власного сервісу скорочення посилань.
